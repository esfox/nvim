return {
  "petertriho/nvim-scrollbar",
  config = function()
    require("scrollbar").setup({
      handle = {
        highlight = "ScrollbarHandle",
      },
      handlers = {
        diagnostic = false,
      },
      marks = {
        GitAdd = {
          text = "│",
        },
        GitChange = {
          text = "│",
        },
      },
    })
  end,
}
